#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
float i,j;
printf("Enter first numer: ");
scanf("%0f",&i);
printf("Enter Second number: ");
scanf("%0f",&j);

if (i>j) 
    printf("First numer is greater than second");
    else
    printf("Second numer is greater than first");
   return 0;
}
